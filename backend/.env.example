# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chess_analyzer
DB_USER=postgres
DB_PASSWORD=

# JWT Configuration
# Generate a strong secret with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=
JWT_EXPIRE_DAYS=7

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@example.com

# Lichess Integration
# Get a token from https://lichess.org/account/oauth/token
LICHESS_TOKEN=

# Frontend URL for CORS
# In development: http://localhost:5173
# In production: https://your-domain.com
FRONTEND_URL=http://localhost:5173

# Set to "development" when running locally over plain HTTP — the guest_id
# cookie (for repertoire access without an account) requires Secure disabled
# in that case, since browsers never send a Secure cookie back over HTTP.
# Leave unset (or any other value) in production, which runs over HTTPS.
APP_ENV=development
